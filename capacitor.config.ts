import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frugal-location-web',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
